#include "gameManager.h"
#include <iostream>

int main(int argc, char* args[])
{
// time to fix Game or stop for now?
// what?
	GameManager manager;
	manager.run();
	std::cout << "Closing program\n";
	return 0;
	//there's one thing we need to look into in run
}